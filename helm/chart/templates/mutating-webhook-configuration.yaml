apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ include "ravendb-operator.fullname" . }}-mutating-webhook-configuration
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "ravendb-operator.fullname" . }}-serving-cert
  labels:
    {{- include "ravendb-operator.labels" . | nindent 4 }}
webhooks:
  - admissionReviewVersions:
      - v1
    clientConfig:
      service:
        name: {{ include "ravendb-operator.fullname" . }}-webhook-service
        namespace: {{ .Release.Namespace }}
        path: /mutate-ravendb-ravendb-io-v1alpha1-ravendbcluster
    failurePolicy: Fail
    name: mravendbcluster.kb.io
    namespaceSelector:
      matchExpressions:
        - key: name
          operator: NotIn
          values:
            - ravendb-operator-system
    rules:
      - apiGroups:
          - ravendb.ravendb.io
        apiVersions:
          - v1alpha1
        operations:
          - CREATE
          - UPDATE
        resources:
          - ravendbclusters
    sideEffects: None
